/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['firebasestorage.googleapis.com','sahara-theme.myshopify.com', 'cloud.appwrite.io'],
      }
};

export default nextConfig;
